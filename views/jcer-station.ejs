<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campus Connect</title>
    <link rel="icon" href="https://qph.cf2.quoracdn.net/main-qimg-c5f5739457afba5604fa49ec261d6303" type="image/x-icon">
    <style>
       /* General Styling */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding-left: 30px;
    padding-right: 30px;
    background-color: #e3f2fd;
    color: #333;
}

h1 {
    text-align: center;
    margin-top: 20px;
    color: #0078d7;
}

/* Add Station Button Styling */
button {
    display: block;
    margin: 20px auto;
    padding: 10px 20px;
    font-size: 16px;
    font-weight: bold;
    color: #fff;
    background-color: #28a745;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: transform 0.2s ease, background-color 0.3s ease, box-shadow 0.3s ease;
}

button:hover {
    background-color: #218838;
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
}

button:active {
    transform: translateY(0);
    background-color: #1e7e34;
}

button:focus {
    outline: none;
    box-shadow: 0 0 0 3px rgba(40, 167, 69, 0.5);
}

/* Card Container */
.card-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 20px;
    padding: 20px;
}

/* Individual Store Card */
.card {
    flex: 1 1 calc(33.33% - 40px); /* Three cards per row */
    max-width: 350px;
    background-color: #fff;
    border: 1px solid #ddd;
    border-radius: 10px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: space-around;
    padding: 15px;
    margin-bottom: 10px;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

/* Store Image Styling */
.card img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    border-bottom: 1px solid #ddd;
}

/* Store Details */
.card h3 {
    font-size: 22px;
    font-weight: bold;
    color: #333;
    margin: 10px 0;
}

.card p {
    margin: 5px 0;
    font-size: 14px;
    color: #555;
}

.card p .rating {
    font-weight: bold;
    color: #f39c12;
}

    </style>
</head>
<body>
    <h1>Stationery Stores</h1>
    <button onclick="window.location.href='http://localhost:8080/home/jcer/station/add'">Add Stationary</button>

    <% if (stores && stores.length > 0) { %>
        <div class="card-container">
            <% stores.forEach(store => { %>
                <div class="card">
                    <img src="<%= store.image %>" alt="Store Image">
                    <h3><%= store.name %></h3>
                    <p><strong>Address:</strong> <%= store.address %></p>
                    <p><strong>Contact:</strong> <%= store.contact_number %></p>
                    <p><strong>Email:</strong> <%= store.email %></p>
                    <p><strong>Hours:</strong> <%= store.hours_of_operation %></p>
                    <p><span class="rating">Rating: <%= store.rating %></span></p>
                    <p><strong>Products:</strong> <%= store.product_name %></p>
                </div>
            <% }) %>
        </div>
    <% } else { %>
        <p>No stores available.</p>
    <% } %>
</body>
</html>
